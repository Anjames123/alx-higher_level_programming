>>> MyList = __import__('1-my_list').MyList

# Test creating a MyList instance with integers
>>> my_list = MyList([1, 2, 3])
>>> print(my_list)
[1, 2, 3]

# Test creating a MyList instance with an empty list
>>> my_list = MyList([])
>>> print(my_list)
[]

# Test creating a MyList instance with a single integer
>>> my_list = MyList([1])
>>> print(my_list)
[1]

# Test creating a MyList instance with non-integer elements
>>> my_list = MyList(['one', 'two', 'three'])
>>> print(my_list)

# Test appending an integer to a MyList instance
>>> my_list = MyList([1, 2])
>>> my_list.append(3)
>>> print(my_list)
[1, 2, 3]

# Test appending a non-integer element to a MyList instance
>>> my_list = MyList([1, 2])
>>> my_list.append('three')
>>> print(my_list)
[1, 2, 'three']
